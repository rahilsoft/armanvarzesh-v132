import React from 'react';
import { ScrollView, Text } from 'react-native';
export default function PrescribePhysioScreen(){
  return (
    <ScrollView contentContainerStyle={{padding:16}}>
      <Text>نسخهٔ فیزیوتراپی (UI نمایشی)</Text>
    </ScrollView>
  );
}
