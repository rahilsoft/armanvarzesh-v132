# Phase K — developer-friendly compose
services:
  backend:
    build: ./app/backend
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=development
  site:
    build: ./app/site
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
  # postgres:
  #   image: postgres:15-alpine
  #   ports: ["5432:5432"]
  #   environment:
  #     - POSTGRES_USER=postgres
  #     - POSTGRES_PASSWORD=postgres
  #     - POSTGRES_DB=app
  #   volumes: [pgdata:/var/lib/postgresql/data]
  # redis:
  #   image: redis:7-alpine
  #   ports: ["6379:6379"]
# volumes:
#   pgdata:
