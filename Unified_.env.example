# Unified environment example for Arman Varzesh
# Generated at 2025-08-12T18:00:41.574155Z

# === Common ===
NODE_ENV=development # [development|staging|production]
APP_NAME=armanvarzesh
TZ=UTC

# === URLs (set https in prod) ===
PUBLIC_BASE_URL=http://localhost:3000
API_BASE_URL=http://localhost:4000
WEB_VITRIN_URL=${PUBLIC_BASE_URL}
GRAPHQL_ENDPOINT=${API_BASE_URL}/graphql
WS_ENDPOINT=ws://localhost:4000/graphql

# === Database ===
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/armanvarzesh
REDIS_URL=redis://localhost:6379/0

# === Auth / Security ===
JWT_ACCESS_SECRET=__CHANGE_ME__
JWT_REFRESH_SECRET=__CHANGE_ME__
JWT_ACCESS_TTL=900s
JWT_REFRESH_TTL=7d
BCRYPT_SALT_ROUNDS=12
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# === Mailing ===
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Arman Varzesh <no-reply@yourdomain.com>"

# === Storage (S3/MinIO) ===
S3_ENDPOINT=http://localhost:9000
S3_REGION=us-east-1
S3_BUCKET=armanvarzesh
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_USE_PATH_STYLE=true

# === Observability ===
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
OTEL_SERVICE_NAME=${APP_NAME}
PROMETHEUS_ENABLED=true

# === Next.js ===
NEXT_PUBLIC_API_BASE=${API_BASE_URL}
NEXT_PUBLIC_GRAPHQL_ENDPOINT=${GRAPHQL_ENDPOINT}
NEXT_PUBLIC_SENTRY_DSN=
NEXT_TELEMETRY_DISABLED=1

# === Expo / Mobile ===
EXPO_PROJECT_ID=
EAS_PROJECT_ID=
EXPO_UPDATE_URL=
EXPO_BUNDLE_IDENTIFIER=com.armanvarzesh.app
EXPO_IOS_SCHEME=armanvarzesh
EXPO_ANDROID_PACKAGE=com.armanvarzesh.app
EXPO_ENABLE_PUSH=true

# === Payments (example) ===
PAYMENT_PROVIDER=stripe
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Matrix:
# DEV:   localhost endpoints; CORS includes http://localhost:3000; JWT_* set to dev secrets
# STAGE: *.staging.yourdomain; CORS restrict to staging web + admin; HTTPS everywhere
# PROD:  *.yourdomain; CORS exact origins; HTTPS; strong JWT secrets; OTEL enabled

# Local data directory for paths previously hardcoded under /mnt
DATA_DIR=/data
