{
  "name": "@arman/rewards-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node dist/main.js",
    "start": "node dist/main.js",
    "build": "tsc -p tsconfig.build.json",
    "test": "jest",
    "coverage": "echo \"no coverage\"",
    "sbom": "npx @cyclonedx/cyclonedx-npm --ignore-npm-errors --json --output-file sbom.json",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate deploy || prisma migrate dev",
    "db:push": "prisma db push",
    "seed": "ts-node prisma/seed.ts || node dist/prisma/seed.js"
  },
  "dependencies": {
    "@nestjs/common": "^10.3.10",
    "@nestjs/core": "^10.3.10",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1",
    "class-validator": "^0.14.0",
    "class-transformer": "^0.5.1",
    "@arman/security-middleware": "workspace:*",
    "@arman/observability-sdk": "workspace:*",
    "@prisma/client": "^5.18.0",
    "compression": "^1.7.4"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "ts-node": "^10.9.2",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.12",
    "prisma": "^5.18.0"
  },
  "engines": {
    "node": ">=20.0.0 <21",
    "pnpm": ">=9 <10"
  }
}