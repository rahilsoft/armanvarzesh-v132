export type Post = { slug:string; title:string; excerpt?:string; body:string; cover?:string; author:string; at:string; tags?:string[] };
export type Page = { slug:string; title:string; body:string };
