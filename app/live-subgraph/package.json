{
  "name": "@arman/live-subgraph",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "typecheck": "tsc -p tsconfig.build.json --noEmit",
    "test": "echo \"No tests yet\" && exit 0",
    "clean": "rimraf dist || rm -rf dist",
    "start:prod": "node dist/main.js",
    "lint": "eslint .",
    "dev": "ts-node src/index.ts || echo \"no dev\"",
    "coverage": "echo \"no coverage\"",
    "sbom": "npx @cyclonedx/cyclonedx-npm --ignore-npm-errors --json --output-file sbom.json"
  },
  "dependencies": {
  "@apollo/server": "^4.10.0",
  "@apollo/subgraph": "^2.7.6",
  "graphql": "^16.9.0",
  "express": "^4.19.2",
  "cors": "^2.8.5",
  "body-parser": "^1.20.2",
  "socket.io": "^4.7.5",
  "uuid": "^9.0.1",
  "@arman/integrations-livekit": "git+https://github.com/rahilsoft/integrations-livekit.git#main",
  "@arman/storage": "git+https://github.com/rahilsoft/storage.git#main",
  "@arman/observability-sdk": "workspace:*",
  "@arman/security-middleware": "workspace:*"
},
  "devDependencies": {
    "typescript": "^5.6.2",
    "@types/node": "^20.12.12",
    "@types/express": "^5.0.3"
  },
  "engines": {
    "node": ">=20.0.0 <21",
    "pnpm": ">=9 <10"
  }
}
