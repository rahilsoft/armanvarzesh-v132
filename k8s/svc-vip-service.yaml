apiVersion: apps/v1
kind: Deployment
metadata:
  name: vip-service
  namespace: armanfit
spec:
  replicas: 2
  selector:
    matchLabels:
      app: vip-service
  template:
    metadata:
      labels:
        app: vip-service
    spec:
      containers:
            a17
              initialDelaySeconds: 20
              periodSeconds: 10
            a17
              initialDelaySeconds: 10
              periodSeconds: 5
        - name: vip-service
          image: ghcr.io/REPLACE_ORG/REPLACE_REPO/vip-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 4117
          env:
            - name: NODE_ENV
              value: "production"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: DATABASE_URL
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: REDIS_URL
            - name: RABBITMQ_URL
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: RABBITMQ_URL
            - name: MINIO_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: MINIO_ENDPOINT
            - name: MINIO_PORT
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: MINIO_PORT
            - name: MINIO_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: MINIO_ACCESS_KEY
            - name: MINIO_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: MINIO_SECRET_KEY
---
apiVersion: v1
kind: Service
metadata:
  name: vip-service
  namespace: armanfit
spec:
  selector:
    app: vip-service
  ports:
    - port: 4117
      targetPort: 4117
      protocol: TCP
  type: ClusterIP