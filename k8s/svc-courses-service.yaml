apiVersion: apps/v1
kind: Deployment
metadata:
  name: courses-service
  namespace: armanfit
spec:
  replicas: 2
  selector:
    matchLabels:
      app: courses-service
  template:
    metadata:
      labels:
        app: courses-service
    spec:
      containers:
            a08
              initialDelaySeconds: 20
              periodSeconds: 10
            a08
              initialDelaySeconds: 10
              periodSeconds: 5
        - name: courses-service
          image: ghcr.io/REPLACE_ORG/REPLACE_REPO/courses-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 4108
          env:
            - name: NODE_ENV
              value: "production"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: DATABASE_URL
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: REDIS_URL
            - name: RABBITMQ_URL
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: RABBITMQ_URL
            - name: MINIO_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: MINIO_ENDPOINT
            - name: MINIO_PORT
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: MINIO_PORT
            - name: MINIO_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: MINIO_ACCESS_KEY
            - name: MINIO_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: MINIO_SECRET_KEY
---
apiVersion: v1
kind: Service
metadata:
  name: courses-service
  namespace: armanfit
spec:
  selector:
    app: courses-service
  ports:
    - port: 4108
      targetPort: 4108
      protocol: TCP
  type: ClusterIP