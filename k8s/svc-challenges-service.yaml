apiVersion: apps/v1
kind: Deployment
metadata:
  name: challenges-service
  namespace: armanfit
spec:
  replicas: 2
  selector:
    matchLabels:
      app: challenges-service
  template:
    metadata:
      labels:
        app: challenges-service
    spec:
      containers:
            a04
              initialDelaySeconds: 20
              periodSeconds: 10
            a04
              initialDelaySeconds: 10
              periodSeconds: 5
        - name: challenges-service
          image: ghcr.io/REPLACE_ORG/REPLACE_REPO/challenges-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 4104
          env:
            - name: NODE_ENV
              value: "production"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: DATABASE_URL
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: REDIS_URL
            - name: RABBITMQ_URL
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: RABBITMQ_URL
            - name: MINIO_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: MINIO_ENDPOINT
            - name: MINIO_PORT
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: MINIO_PORT
            - name: MINIO_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: MINIO_ACCESS_KEY
            - name: MINIO_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: MINIO_SECRET_KEY
---
apiVersion: v1
kind: Service
metadata:
  name: challenges-service
  namespace: armanfit
spec:
  selector:
    app: challenges-service
  ports:
    - port: 4104
      targetPort: 4104
      protocol: TCP
  type: ClusterIP