
name: Test Admin Panel

on: [push, pull_request]

jobs:
  admin-panel-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js 18
        uses: actions/setup-node@v4
        with:
          node-version: 18
      - name: Install deps
        run: cd admin-panel && npm ci
      - name: Run tests
        run: cd admin-panel && npm run test:cov
